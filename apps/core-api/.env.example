# Copy to .env (gitignored). Local Docker Postgres matches docker-compose.yml at repo root.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/sphincs_erp_crm?schema=public"

JWT_SECRET="change-me-local-dev-only"
APP_VERSION="Beta V1.16.77"
NODE_ENV="development"
BUILD_HASH="local-docker-postgres"

# Optional: GitHub Issues for bug reports (see docs/deployment.md)
# GITHUB_ISSUES_TOKEN=
# GITHUB_ISSUES_REPO=
