# Worklog — 30-04-2026

## Tasks completed

### Demo database seed (core-api)

- Implemented an expanded **Prisma demo seed** in `apps/core-api/prisma/seed/demo-graph.ts`, invoked from `apps/core-api/prisma/seed.ts` after the existing baseline (org, roles, admin, permissions).
- Seeded **implemented domain models** with linked rows (two branches, items, suppliers, PO + lines, CRM chain, warehouse locations, inventory stocks, movements, goods receipt + lot + balance, stock transfer, adjustment, dispatch + pick/pack jobs, return, reservation, reorder rule, stock alert, audit log). **`refresh_tokens`** intentionally omitted (runtime session table).
- Aligned seed timestamps with **Postgres temporal guardrails** on `goods_receipts`, `stock_transfers`, and `stock_adjustments`.
- Documented the seed contract in [database-schema.md](./database-schema.md) and cross-linked from [setup.md](./setup.md).

### Unified ERP + CRM web (primary UX)

- Merged **CRM screens** (Contacts, Leads, Opportunities) into **`apps/erp-web`** with the same hash routes: `#/contacts`, `#/leads`, `#/opportunities`.
- Single shell: sidebar **SPHINCS**, ERP links when user has ERP roles, CRM links when user has CRM roles; top bar **ERP Operations** vs **CRM Operations** from path; in-app **Link** navigation for ERP/CRM (no second origin for normal use).
- **`CrmAccessGate`** preserves CRM-only access messaging; **`defaultAppLandingPath`** sends ERP-capable users to `/items`, CRM-only to `/contacts`; **`useEffect`** redirects when `/auth/me` sync changes roles while the current route is no longer allowed.
- Opportunities handoff help text uses **`Link` to `/purchase-orders`** (same app).
- **`apps/crm-web`** reduced to a **redirect stub** (`main.ts` + `redirect.ts`): preserves hash and sends to **`VITE_ERP_WEB_URL`** (dev default `http://localhost:5173`); Vitest unit tests for URL builder; removed React bundle from CRM package.

### UI and API developer experience

- **`packages/ui-core`**: `.app-sidebar` uses **`display: flex`** and **`flex-direction: column`** for consistent vertical nav.
- **`packages/ui-core`**: devDependencies **`@types/react`** + **`typescript`** so workspace **`tsc`** can type-check TSX consumers.
- **`apps/erp-web`**: **`typecheck`** script (`tsc --noEmit`); **`app.tsx`** fixes (legacy storage loop, `PurchaseOrderRecord.created_at` typing for `toDateInputValue`).
- **`apps/core-api`**: default **CORS** includes **`http://127.0.0.1:5173`** and **`:5174`**; **`.env.example`** documents **`PORT=3000`**.
- **`apps/erp-web/.env.example`**: documents **`VITE_API_BASE_URL`** aligned with core-api (port 3000).

### Documentation

- Updated [setup.md](./setup.md), [frontend-guide.md](./frontend-guide.md), [architecture.md](./architecture.md), root [index.md](../index.md), [docs/index.md](./index.md), and [CHANGELOG.md](../CHANGELOG.md) for unified app behavior and local dev expectations.

## Problems encountered

- Seed: first run failed on `goods_receipts` check constraint **`ck_goods_receipts_received_date_order`**; fixed with ordered timestamps.
- Local dev: **`ERR_CONNECTION_REFUSED`** when **`VITE_API_BASE_URL`** pointed at the wrong port or **core-api** was not running; **`EADDRINUSE`** on port 3000 when **`nest --watch`** left a stale **node** process — free the port or use a single API dev process.

## Decisions made

- Keep CRM package as a **compatibility redirect** for bookmarks and **`localhost:5174`** instead of deleting the workspace entry immediately.
- Scope “seed everything” to **implemented Prisma tables**, not the full **107** blueprint tables.

## Next steps

- Exercise list/read APIs against the demo dataset; extend seed permissions if new endpoints require them.
- Optional: split **`apps/erp-web/src/app.tsx`** into route modules when maintainability demands it.
- Continue blueprint-driven Prisma work for missing blueprint tables when prioritized.
